<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug: trust user-installed CAs (for mitmproxy / charles during dev). -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <!-- Cleartext HTTP: local dev loopbacks + Lightsail origin (until real TLS
         cert is in place; user directive 2026-04-20 to target http://IP). -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">54.252.241.150</domain>
        <!-- Mac's Personal Hotspot bridge IP (phone reaches Mac as 192.168.2.1). -->
        <domain includeSubdomains="false">192.168.2.1</domain>
        <!-- Standard home-WiFi Mac IP — harmless to whitelist both. -->
        <domain includeSubdomains="false">192.168.1.47</domain>
        <trust-anchors>
            <certificates src="@raw/abtaxi_prod" />
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Default: HTTPS only, system CAs. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
