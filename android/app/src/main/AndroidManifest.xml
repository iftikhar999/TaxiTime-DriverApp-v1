<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- Android 14+ requires a type-specific permission for foreground services.
         notifee's keepalive service uses `dataSync` so dispatcher messages keep
         flowing while the app is backgrounded. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" /> <!-- ✅ For auto-restart after reboot -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" /> <!-- ✅ CRITICAL: Prevent Android from killing the app -->
    <uses-permission android:name="android.permission.WAKE_LOCK" /> <!-- ✅ Keep device awake for location updates -->
    <!-- ✅ Storage for document upload (driver docs: license, insurance, vehicle reg).
         Android 13+ uses READ_MEDIA_IMAGES; older uses READ_EXTERNAL_STORAGE. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-feature android:name="android.hardware.location" />
    <uses-feature android:name="android.hardware.location.gps" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />

    <!-- Stripe Tap to Pay on Android: NFC + Bluetooth are required by the
         Terminal SDK even when only using Tap-to-Pay. Marked not required so
         the app still installs on devices without NFC (the payment method is
         hidden at runtime when unsupported). -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true"
      android:networkSecurityConfig="@xml/network_security_config">

      <!-- Google Maps API key (replace value before shipping) -->
      <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="AIzaSyBhcA7J8ZefAwlzhuYUNDIf_W3Yzy_16gA" />
      
      <!-- Location Tracking Service -->
      <service
        android:name="com.taxitime.driverapp.location.LocationTrackingService"
        android:exported="false"
        android:foregroundServiceType="location" />
      
      <!-- ✅ Job Meter Service for background job tracking -->
      <service
        android:name="com.taxitime.driverapp.meter.JobMeterService"
        android:exported="false"
        android:foregroundServiceType="location" />
      
      <!-- ✅ Foreground Service for keeping app alive during shift -->
      <service
        android:name=".ForegroundService"
        android:exported="false"
        android:foregroundServiceType="location" />
      
      <!-- ✅ Broadcast Receiver for auto-restart -->
      <receiver
        android:name=".RestartServiceBroadcast"
        android:exported="false">
        <intent-filter>
          <action android:name="com.taxitime.driverapp.RESTART_SERVICE" />
        </intent-filter>
      </receiver>
      
      <!-- ✅ Boot Receiver to restart service after device reboot -->
      <receiver
        android:name=".BootReceiver"
        android:exported="true"
        android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
        <intent-filter>
          <action android:name="android.intent.action.BOOT_COMPLETED" />
          <action android:name="android.intent.action.QUICKBOOT_POWERON" />
        </intent-filter>
      </receiver>
      
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:showWhenLocked="true"
        android:turnScreenOn="true"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- Override notifee's default foreground service so it runs as
           `dataSync` (no 3-minute limit) instead of Android 14+'s default
           SHORT_SERVICE, which triggers an ANR after the timeout on our
           never-resolving keep-alive task. tools:replace overrides the
           manifest-merge value set by @notifee/react-native. -->
      <service
          android:name="app.notifee.core.ForegroundService"
          android:foregroundServiceType="dataSync"
          android:stopWithTask="true"
          tools:replace="android:foregroundServiceType" />
    </application>
</manifest>
